import React from 'react';
import HeadingPrimary from '../Headings/HeadingPrimary';

const MainTitle = () => {
  return (
    <HeadingPrimary mainText={'Nature'} subText={'Is worth to discover'} />
  );
};

export default MainTitle;
